name: Use git App

on: 
  push:
    paths:
      - .github/workflows/build.yml

jobs:
  clone-other-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ vars.GH_APPS_ID }}
          private-key: ${{ secrets.GH_APPS_KEY }}    
      # - name: Get Token
      #   id: get_workflow_token
      #   uses: peter-murray/workflow-application-token-action@v4
      #   with:
      #     application_id: ${{ vars.GH_APPS_ID }}
      #     application_private_key: ${{ secrets.GH_APPS_KEY }}
          
      - uses: actions/checkout@v4
        with:
          token: ${{ steps.get_workflow_token.outputs.token }}
          repository: 'congneb/test_build'
      
      - name: other clone
        run: |
          git clone https://x:${{ steps.get_workflow_token.outputs.token }}@github.com/congneb/test_build haha
          ls haha
        
        

